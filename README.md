To run application - docker-compose up

```
lets create the jenkin pipeline
```

### To run application - docker-compose up

## To run application - docker-compose up

# To run application - docker-compose up
